[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wise-product-intelligence"
version = "0.1.0"
description = "RC7 product intelligence recommendations for commercial validation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/wise_product_intelligence"]
