[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    smoke: Fast health-check and import smoke tests
    integration: Tests requiring docker-compose infrastructure
    e2e: End-to-end Reference Capability acceptance tests
