[project]
name = "wise"
version = "0.1.0"
description = "WISE platform monorepo"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: requires PostgreSQL (WISE_TEST_DATABASE_URL)",
]
